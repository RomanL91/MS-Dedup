"""Разбор пользовательского ввода и построение ссылок API МойСклад.

Чистые функции без побочных эффектов и сетевых вызовов:

* :mod:`app.parsing.identifiers` — извлечение UUID и определение типа сущности по ссылке;
* :mod:`app.parsing.hrefs` — построение и разбор API-ссылок (href) сущностей.

Эти функции не зависят ни от Redis, ни от HTTP, поэтому легко тестируются.
"""
