"""Ядро приложения: инфраструктурные примитивы, не зависящие от предметной области.

Пакет содержит то, что нужно почти всем остальным слоям:

* :mod:`app.core.config` — конфигурация приложения (``settings``);
* :mod:`app.core.broker` — брокер очереди задач taskiq;
* :mod:`app.core.redis` — фабрика и контекстный менеджер подключений к Redis.

Слой ``core`` не должен зависеть ни от ``domain``, ни от вышестоящих слоёв —
зависимости направлены только внутрь, к нему.
"""
