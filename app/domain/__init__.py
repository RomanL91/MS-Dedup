"""Предметная область (domain) сервиса дедупликации.

Здесь живут понятия, не зависящие ни от транспорта (HTTP/Redis), ни от UI:

* :mod:`app.domain.enums` — перечисления статусов, типов сущностей и режимов очистки;
* :mod:`app.domain.models` — Pydantic-модели состояния (прогресс, позиции, метаданные);
* :mod:`app.domain.entity_types` — реестр человекочитаемых названий типов сущностей;
* :mod:`app.domain.documents` — типы документов МойСклад, где выполняется замена.

Слой domain зависит только от ``core`` и стандартной библиотеки.
"""
